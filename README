主题使用须知：
将Typecho目录内var/Typecho/Widget/Helper/PageNavigator/Box.php文件
的195行$itemBegin注释掉，添加这一行
$itemBegin = empty($itemTag) ? '' : ('<' . $itemTag . (empty($itemClass) ? '' : ' class="' . $itemClass . '"') . '>');
